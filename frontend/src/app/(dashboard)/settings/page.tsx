export default function SettingsPage() {
  return (
    <section className="module-card">
      <span className="eyebrow">Later Module</span>
      <h1>Settings</h1>
      <p>This placeholder keeps the route structure stable without adding product logic yet.</p>
    </section>
  );
}
